'use client';

// DevActivityTable is superseded by DevActivityCharts which includes a task selector.
// Kept as a stub to avoid breaking imports.
export function DevActivityTable() {
  return null;
}
